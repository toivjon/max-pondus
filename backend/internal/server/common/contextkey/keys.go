package contextkey

// RequestID is a key for unique identifiers assigned to each incoming request.
const RequestID ContextKey = "requestID"

// User is a key for the user information container assigned for each authenticated request.
const User ContextKey = "user"
